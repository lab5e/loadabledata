/**
 * LoadingState is the different states available for a loading lifecycle
 */
export type LoadingState = "ready" | "loading" | "error" | "none";

/**
 * CreateState is the different states available for a loading lifecycle
 */
export type CreateState = "none" | "loading" | "error" | "success";
